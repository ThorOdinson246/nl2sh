name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  test:
    name: test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install whatisit with dev extras
        working-directory: whatisit_pkg
        run: python -m pip install -e ".[dev]"

      - name: Lint with ruff
        working-directory: whatisit_pkg
        run: ruff check .

      - name: Run tests with coverage
        working-directory: whatisit_pkg
        run: pytest -q --cov=whatisit --cov-report=term-missing

  build-and-install:
    name: build & install (clean venv)
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build backend
        run: python -m pip install build

      - name: Build sdist and wheel
        working-directory: whatisit_pkg
        run: python -m build

      - name: Install the built wheel into a fresh venv
        run: |
          python -m venv /tmp/whatisit-install-test
          /tmp/whatisit-install-test/bin/pip install whatisit_pkg/dist/*.whl

      - name: whatisit --help
        run: /tmp/whatisit-install-test/bin/whatisit --help

      - name: "whatisit doctor (expected to exit 1 gracefully, no model present)"
        run: |
          # doctor must exit non-zero when no model/runtime is installed, but
          # it must NOT crash. Assert exactly that: rc==1, not an exception.
          set +e
          /tmp/whatisit-install-test/bin/whatisit doctor
          rc=$?
          set -e
          echo "doctor exit code: $rc"
          if [ "$rc" -ne 1 ]; then
            echo "expected doctor to exit 1 (not ready, no crash); got $rc" >&2
            exit 1
          fi
